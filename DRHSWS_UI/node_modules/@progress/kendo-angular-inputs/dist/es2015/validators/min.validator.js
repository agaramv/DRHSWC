/**
 * @hidden
 */
export const createMinValidator = (minValue) => {
    return (c) => {
        const err = {
            minError: {
                minValue: minValue,
                value: c.value
            }
        };
        return (c.value !== null && c.value < minValue) ? err : null;
    };
};
