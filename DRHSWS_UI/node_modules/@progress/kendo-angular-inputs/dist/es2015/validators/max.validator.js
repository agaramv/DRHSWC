/**
 * @hidden
 */
export const createMaxValidator = (maxValue) => {
    return (c) => {
        const err = {
            maxError: {
                maxValue: maxValue,
                value: c.value
            }
        };
        return (c.value !== null && c.value > maxValue) ? err : null;
    };
};
