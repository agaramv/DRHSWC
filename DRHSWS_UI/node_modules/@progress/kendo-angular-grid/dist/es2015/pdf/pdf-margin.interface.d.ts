import { PageMargin } from '@progress/kendo-drawing/pdf';
/**
 * Specifies the margins of the page (numbers or strings with units).
 *
 * The supported units are:
 * * `"mm"`
 * * `"cm"`
 * * `"in"`
 * * `"pt"` (default)
 */
export interface PDFMargin extends PageMargin {
}
