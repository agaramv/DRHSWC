import { ElementRef, EventEmitter, NgZone, OnDestroy, OnInit } from '@angular/core';
import { DraggableDirective } from '../common/draggable.directive';
import { DragAndDropService } from './drag-and-drop.service';
import { DragHintService } from './drag-hint.service';
import { DragAndDropContext } from './context-types';
import { DropCueService } from './drop-cue.service';
import { NavigationService } from '../navigation/navigation.service';
/**
 * @hidden
 */
export declare class DraggableColumnDirective implements OnInit, OnDestroy {
    draggable: DraggableDirective;
    element: ElementRef;
    private zone;
    private service;
    private hint;
    private cue;
    private nav;
    context: DragAndDropContext;
    kendoDraggableColumn: boolean;
    drag: EventEmitter<any>;
    private subscriptions;
    private enabled;
    constructor(draggable: DraggableDirective, element: ElementRef, zone: NgZone, service: DragAndDropService, hint: DragHintService, cue: DropCueService, nav: NavigationService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private drop(upEvent);
    private performDrag({move});
    private elementUnderCursor(mouseEvent);
}
